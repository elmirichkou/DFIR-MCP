services:
  dfir-backend:
    build: ./backend
    container_name: dfir-mcp-backend
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./images:/images:ro
      - ./symbols:/symbols:ro
    restart: unless-stopped
